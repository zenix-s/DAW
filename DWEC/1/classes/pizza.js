export default class Pizza{
  constructor(
    size,
    ingridients,
  ){
    this.size = size
    this.ingridients = ingridients
  }

  /**
   * 
   * @returns int precio de la pizza
   * 
   */
  calcPrice(){
    const price = 2
    
    return price
  }
}